import math,cmath

print 'funcs in math module:\n'
print dir(math)

print 'funcs in cmath module:\n'
print dir(cmath)

print 'MATH FUNCTION:\n'
print 'abs(x) / cell(x) / cmp(x,y) / fabs / floor /log / log10(x)\nmax(x1,x2..) / min(x1,xx2,..) / modf(x) / pow(x,y) / round (x,[n]) / sqrt'

print 'RANDOM FUNCTION:\n'
print 'choice(sequence) / randrange([start],stop[,step]) / random(x) #range in [0,1]\nseed([x]) / shuffles(list) / uniform(x,y)'

